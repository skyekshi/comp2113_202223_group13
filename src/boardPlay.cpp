#include "boardPlay.hpp"


namespace Game {
    namespace {
        enum Directions { UP, DOWN, RIGHT, LEFT };

        gameBoard moveOnce(Directions d, gameBoard gb) {
            switch(d) {
                case UP:

                case DOWN:

                case LEFT:

                case RIGHT:
            }
            return gb;
        }
        
        //task 3 for tunneling
        gameBoard boardTunneling(Directions d, gameBoard gb) {

        }

        gameBoard boardBarrier() {
            
        }

    }
}